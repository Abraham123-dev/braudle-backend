// Global error handling middleware
// Already defined in app.js but this shows the pattern

class ErrorHandler {
  static handle(err, req, res, next) {
    // Implementation goes here when needed
  }
}

export { ErrorHandler };
